import QtQuick

// Phase 1 stub — replaced with the full overlay in Phase 3.
Item {
  id: root

  property var shell: null
  property var manifest: null

  function open(payloadJson) {}
  function close() {}
  function toggle() {}
}
